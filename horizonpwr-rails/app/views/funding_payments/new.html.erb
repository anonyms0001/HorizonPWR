<div class="container px-4 mx-auto my-8">
  <div class="max-w-3xl mx-auto">
    <div class="flex items-center justify-between mb-4">
      <h1 class="h3"><%= t("shared.scaffold.new.title", model: "Funding Payment") %></h1>
      <%#= link_to t("cancel"), funding_payments_path, class: "btn btn-link" %>
    </div>

    <div class="p-8 bg-white rounded shadow">
      <%= render "form", funding_payment: @funding_payment, url: project_funding_funding_payments_path(funding_id: @funding.id) %>
    </div>
  </div>
</div>
