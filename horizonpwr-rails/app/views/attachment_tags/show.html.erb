<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to "Attachment Tags", attachment_tags_path %> > Attachment Tag
        #<%= @attachment_tag.id %></h1>
      <%= link_to t("edit"), edit_attachment_tag_path(@attachment_tag), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
      <div class="mb-4">
        <h5>Index</h5>
        <%= @attachment_tag.index %>
      </div>
      <div class="mb-4">
        <h5>Show</h5>
        <%= @attachment_tag.show %>
      </div>
      <div class="mb-4">
        <h5>Edit</h5>
        <%= @attachment_tag.edit %>
      </div>
      <div class="mb-4">
        <h5>New</h5>
        <%= @attachment_tag.new %>
      </div>
    </div>
  </div>
</div>
