<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to "Form Responses", form_responses_path %> > Form Response #<%= @form_response.id %></h1>
      <%= link_to t("edit"), edit_form_response_path(@form_response), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Respondable</h5>
        <%= @form_response.respondable %>
      </div>
            <div class="mb-4">
        <h5>Dynamic form</h5>
        <%= @form_response.dynamic_form %>
      </div>

    </div>
  </div>
</div>
