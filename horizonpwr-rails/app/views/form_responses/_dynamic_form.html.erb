<section>
  <div class="overflow-hidden shadow-lg rounded-lg bg-white ">
    <div class="sm:rounded-tr-none relative group bg-white">
      <!-- <p class="text-xl font-medium text-gray-900 sm:text-2xl">Pre Install</p>-->
      <!--TODO: ADD CUSTOM ELEMENTS HERE FOR THIS SECTION-->
      <div>
<!--        <div class="sm:hidden">-->
<!--          <form>-->
<!--            <label for="tab" class="sr-only">Select a tab</label>-->
<!--            <select id="tab" name="tab" class="block w-full border-gray-300 rounded-t-md" onchange="this.form.submit()">-->
              <%# dynamic_form.form_configs.each do |form_config| %>
<!--                <option <%#= !params.has_key?('tab') || params['tab'] == "#{form_config.title}" ? "selected=selected value=#{form_config.title}" : "value=#{form_config.title}" %> >-->
                  <%#= form_config.title %>
<!--                </option>-->
              <%# end %>
<!--            </select>-->
<!--          </form>-->
<!--        </div>-->
        <div class="hidden sm:block">
          <nav class="relative z-0 shadow-sm flex divide-x divide-gray-200" aria-label="Tabs">
            <% dynamic_form.form_configs.each do |form_config| %>
              <%= link_to project_path(@project.id, :tab => "#{form_config.title}"), class: "text-gray-900 group relative min-w-0 flex-1 overflow-hidden bg-white py-4 px-4 text-sm font-medium text-center hover:bg-gray-100 focus:z-10" do %>
                          <span>
                            <%= form_config.title %>
                          </span>
                <% if !params.has_key?('tab') || params['tab'] == "#{form_config.title}" %>
                  <span aria-hidden="true" class="bg-indigo-500 absolute inset-x-0 bottom-0 h-0.5"></span>
                <% end %>
              <% end %>
            <% end %>
          </nav>
        </div>

        <div>
          <% dynamic_form.form_configs.limit(1).each do |form_config| %>
            <h1 class="text-center py-4">
              <%= form_config.title %>
              <%#= !params.has_key?('tab') || params['tab'] == "#{form_config.title}" ? "#{form_config.title}" : params['tab']%>
            </h1>
            <%= render 'form_responses/form', form_config: form_config, form_response: @form_response %>
          <% end %>

        </div>
      </div>
    </div>
  </div>
</section>