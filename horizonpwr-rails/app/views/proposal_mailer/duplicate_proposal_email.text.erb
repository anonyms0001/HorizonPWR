Hey <%= @user.first_name %>,

  This is an automated message from PWRStation:

  Duplicate proposals have been created for <%= @proposal.appointment.address.contacts.first.name %>. They have been both been marked as blocked.

  We urge you to promptly delete the unneeded proposal, so work can proceed.
