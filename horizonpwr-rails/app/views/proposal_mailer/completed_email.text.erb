Hey <%= @user.first_name %>,

  <%= "The proposal for your #{@proposal.appointment.start_at.strftime("%b %e, %Y, %l:%M %P")} appointment with #{@contact.name} has been completed." %>

  <%= @url %>

  Reach out to the support team if you have questions.
