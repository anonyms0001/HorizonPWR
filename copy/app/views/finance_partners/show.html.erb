<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= link_to "Finance Partners", finance_partners_path %> > Finance Partner #<%= @finance_partner.id %></h1>
      <%= link_to t("edit"), edit_finance_partner_path(@finance_partner), class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
            <div class="mb-4">
        <h5>Name</h5>
        <%= @finance_partner.name %>
      </div>
          </div>
  </div>
</div>
