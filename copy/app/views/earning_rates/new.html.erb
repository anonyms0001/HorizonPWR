<div class="container mx-auto my-8 px-4">
  <div class="max-w-3xl mx-auto">
    <div class="flex justify-between items-center mb-4">
      <h1 class="h3"><%= t("shared.scaffold.new.title", model: "Earning Rate") %></h1>
      <%= link_to t("cancel"), earning_rates_path, class: "btn btn-link" %>
    </div>

    <div class="bg-white rounded shadow p-8">
      <%= render "form", earning_rate: @earning_rate %>
      <%#= redirect_back(earning_rate: @earning_rate, notice: "You have errors man!!!") %>
    </div>
  </div>
</div>
