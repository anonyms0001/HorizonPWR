<%= "Hey #{@appointment.scheduled_with.name}," %>
  <%= "#{@appointment.created_by.name}, scheduled a consultation appointment with #{@customer} for  #{@appointment.date.strftime("%b %e, %Y, %l:%M %P")}" %>